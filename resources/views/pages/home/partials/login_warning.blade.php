<section id="post-login-warning" class="mt-4">
    <div class="container">
        <blockquote class="blockquote shadow-sm text-warning border border-warning p-4 rounded bg-light">
            <p>
                <strong>Warning:</strong> To post anything, you need to
                <a href="{{route('login')}}" class="btn btn-warning btn-sm text-white">Login</a> or
                <a href="{{route('register')}}" class="btn btn-outline-warning btn-sm">Signup</a>.
            </p>
        </blockquote>
    </div>
</section>
